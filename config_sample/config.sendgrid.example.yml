probes:
  - id: probe-01
    requests:
      - url: http://github.com

notifications:
  - id: unique-id-mailgun
    type: sendgrid
    data:
      recipients: [RECIPIENT_EMAIL_ADDRESS]
      apiKey: YOUR_API_KEY
      sender: YOUR_VERIFIED_EMAIL_BY_SENDGRID
